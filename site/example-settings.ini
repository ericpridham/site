[template]

template_dir = html/
compile_dir  = var/compile/
config_dir   = var/config/
cache_dir    = var/cache/

[database]

host = localhost
username = username
password = password
database = database

[model]

models_path = inc/models/
